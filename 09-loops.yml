- name: Demo on loops
  hosts: all
  task:
    - name: Printing the user details 
      ansible.builtin.debug:  
        msg: "value of item {{item}}"
      loop:
        - 300
        - 700
        - 100